@model IEnumerable<Domain.App.AdDesignInReservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AdDesign)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdDesign.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reservation.CampaignName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdDesignInReservationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdDesignInReservationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdDesignInReservationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
